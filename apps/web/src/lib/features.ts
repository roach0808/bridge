/**
 * Availability (working hours) is hidden in the web app to keep it simple:
 * Experts add only time off, and the New Call form does not check working
 * hours. The API, database and tests keep full support; flip this to bring it back.
 */
export const AVAILABILITY_ENABLED = false;
